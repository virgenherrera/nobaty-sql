export const USE_REAL_TIME_SERVICE = false;
export const USE_DATA_PERSISTENCE = true;

export const AVAILABLE_ENVIRONMENTS = ['development', 'test', 'production'];

export const DEFAULT_PAGINATION = {
	limit: 50,
	offset: 0,
};
